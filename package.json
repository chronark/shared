{
  "name": "@chronark/shared",
  "private": true,
  "npmClient": "yarn",
  "scripts": {
    "fmt": "lerna run fmt && prettier --write --loglevel=warn --ignore-path=.gitignore .",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.3.2",
    "typescript": "^4.3.5"
  }
}
